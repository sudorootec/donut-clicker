{
  "version": 2,
  "builds": [
    {
      "src": "bin/www",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "bin/www" }
  ],
  "env": {
    "MONGODB_URI": "@sudorootec/DonutClicker-NDAP-MONGODB_URI"
  },
  "build": {
    "env": {
      "MONGODB_URI": "@sudorootec/DonutClicker-NDAP-MONGODB_URI"
    }
  }
}
 
